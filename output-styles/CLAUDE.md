# Output Styles

This directory contains Claude output-style docs and local guidance for plugin-lab.

Read the mirrored docs in `output-styles/docs/` before creating or editing output styles. Refresh them with `scripts/update-doc-mirrors.sh`.
