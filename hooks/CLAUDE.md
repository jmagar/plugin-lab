# `hooks/`

Read the relevant hook docs before creating or editing hooks here.

## Docs

- Claude hook docs: `hooks/docs/claude/`
- Codex hook docs: `hooks/docs/codex/`

## Refresh

Use `scripts/update-doc-mirrors.sh` from the repo root to refresh mirrored markdown docs.
