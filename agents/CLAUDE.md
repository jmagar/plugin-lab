# `agents/`

Read the relevant agent docs before creating or editing agents here.

## Docs

- Claude agent docs: `agents/docs/claude/`
- Codex agent docs: `agents/docs/codex/`

## Refresh

Use `scripts/update-doc-mirrors.sh` from the repo root to refresh mirrored markdown docs.
