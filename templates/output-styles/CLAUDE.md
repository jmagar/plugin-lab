# Output Styles Templates

This directory mirrors the scaffolded output-style docs used by generated plugins.

Read the mirrored docs in `templates/output-styles/docs/` before changing template output styles. Refresh them with `scripts/update-doc-mirrors.sh`.
