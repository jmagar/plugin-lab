# `templates/hooks/`

Read the relevant hook docs before creating or editing hook templates here.

## Docs

- Claude hook docs: `templates/hooks/docs/claude/`
- Codex hook docs: `templates/hooks/docs/codex/`

## Refresh

Use `scripts/update-doc-mirrors.sh` from the repo root to refresh mirrored markdown docs.
