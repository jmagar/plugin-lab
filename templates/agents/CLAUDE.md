# `templates/agents/`

Read the relevant agent docs before creating or editing agent templates here.

## Docs

- Claude agent docs: `templates/agents/docs/claude/`
- Codex agent docs: `templates/agents/docs/codex/`

## Refresh

Use `scripts/update-doc-mirrors.sh` from the repo root to refresh mirrored markdown docs.
